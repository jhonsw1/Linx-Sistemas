CREATE PROCEDURE [DBO].[LX_CADE] @TEXTO VARCHAR(100)=NULL 
AS
	

IF SUBSTRING(@TEXTO,1,1) <> '%'
		
SELECT @TEXTO = '%'+@TEXTO
	
IF SUBSTRING(@TEXTO,DATALENGTH(@TEXTO),1) <> '%'
		
SELECT @TEXTO = @TEXTO+'%'
	
SELECT * FROM SYSOBJECTS WHERE NAME LIKE @TEXTO AND TYPE = 'U'
