IF OBJECT_ID('DBO.LOJA_PEDIDO_PROMO','U')  IS NULL 
BEGIN
	CREATE TABLE DBO.LOJA_PEDIDO_PROMO
	(
		CODIGO_FILIAL_ORIGEM	   CHAR(6)		  NOT NULL,
		PEDIDO	                   INT			  NOT NULL,		
		TERMINAL				   CHAR(3)		  NOT NULL,	
		DATA_SESSION               DATETIME		  NOT NULL,
		OPTION_PROMO			   INT			  NOT NULL,
		MAPVERSION				   CHAR(06)       NOT NULL,
		VL_BENEF_TOTAL			   NUMERIC(9,2)   NOT NULL,
		ID_TRANSACTION             VARCHAR(100)   NULL,
		INATIVO					   BIT DEFAULT(0) NULL,
		VOUCHER					   VARCHAR(100)	  NULL,
		COUPONID				   VARCHAR(100)   NULL,
		JSON_REQUEST			   VARCHAR(MAX)   NULL,
		JSON_RESPONSE			   VARCHAR(MAX)   NULL,
		DESCONTO				   NUMERIC(9,2)   NULL,
		DATA_PARA_TRANSFERENCIA	   DATETIME	NULL DEFAULT getdate(),
		CONSTRAINT  XPKLOJA_PEDIDO_PROMO PRIMARY KEY (CODIGO_FILIAL_ORIGEM, PEDIDO, TERMINAL, DATA_SESSION)
	)
END